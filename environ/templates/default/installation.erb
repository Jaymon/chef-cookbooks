# Installed by <%= @name %> Chef Cookbook, any direct changes will most likely be
# overwritten on next Chef run, configuration can be done through Chef's environment
# node["<%= @name %>"] value.
set -a
if which source; then
  source <%= @path %>
else
  . <%= @path %>
fi
set +a

